// The external booking page (Google Appointment Scheduling). One constant so
// every CTA links the same target — render it as a real <a href> (crawlable,
// middle/cmd-click friendly), not a window.open() handler.
export const BOOKING_URL = "https://calendar.app.google/kFaanhSae5WefLnD7";
